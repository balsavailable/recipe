import { Decimal2Directive } from './decimal2.directive';

describe('Decimal2Directive', () => {
  it('should create an instance', () => {
    const directive = new Decimal2Directive();
    expect(directive).toBeTruthy();
  });
});
