import { Dummy2Directive } from './dummy2.directive';

describe('Dummy2Directive', () => {
  it('should create an instance', () => {
    const directive = new Dummy2Directive();
    expect(directive).toBeTruthy();
  });
});
